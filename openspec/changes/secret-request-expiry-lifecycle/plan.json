{
  "change": "secret-request-expiry-lifecycle",
  "project": "doriath",
  "repo": "ConductionNL/doriath",
  "base_branch": "development",
  "feature_branch": "feature/268/secret-request-expiry-lifecycle",
  "created": "2026-08-18",
  "tracking_issue": 268,
  "specs": [
    "secret-requests"
  ],
  "tasks": [
    {
      "id": 1,
      "number": "1.1",
      "section": "Set an expiry",
      "title": "Pre-fill the expiry field in `SecretRequestCreateDialog.vue` with a suggested value the requester can change or clear.",
      "status": "done",
      "acceptance_criteria": [
        "Confirm the interval with the product owner before implementing; the spec asserts only that it is suggested and clearable",
        "Clearing it MUST produce a request with `expires_at` null, so a perpetual link stays available in one action",
        "A pre-filled value must survive the dialog's reset/reopen cycle correctly rather than reverting to empty"
      ]
    },
    {
      "id": 2,
      "number": "2.1",
      "section": "Act on an expiry",
      "title": "Add the terminal `STATUS_EXPIRED` to `SecretRequest`.",
      "status": "done",
      "acceptance_criteria": [
        "`status` is already a string column, so no migration is required"
      ]
    },
    {
      "id": 3,
      "number": "2.2",
      "section": "Act on an expiry",
      "title": "Add the expire transition to `SecretRequestService`: invalidate the token, delete the placeholder for a fresh request, preserve the Secret and its values for a re-request, and dispatch the audit event with the SYSTEM as actor rather than the requester.",
      "status": "done",
      "acceptance_criteria": []
    },
    {
      "id": 4,
      "number": "2.3",
      "section": "Act on an expiry",
      "title": "Add a `TimedJob` that sweeps lapsed requests and register it in `appinfo/info.xml`, following `ExpireMachineLeasesJob` (hourly).",
      "status": "done",
      "acceptance_criteria": [
        "Query is narrow: `status = pending` AND `expires_at IS NOT NULL` AND `expires_at < now`",
        "A request with no `expires_at` is never touched",
        "The job body must do real work; an empty `run()` trips the stub-scan gate"
      ]
    },
    {
      "id": 5,
      "number": "3.1",
      "section": "Enforce it independently of the sweeper",
      "title": "Hoist the expiry evaluation in `SecretRequestPolicy::requireOpenByToken()` above the status switch so it applies to any status.",
      "status": "done",
      "acceptance_criteria": [
        "A request that lapsed since the last sweep is refused on `expires_at` alone \u2014 the job is cleanup, never enforcement"
      ]
    },
    {
      "id": 6,
      "number": "3.2",
      "section": "Enforce it independently of the sweeper",
      "title": "Give `expired` an explicit arm in that switch, in the 410 family alongside `fulfilled` and `declined`.",
      "status": "done",
      "acceptance_criteria": [
        "Without it the new status falls into `default: \u2026 'Request is in an unknown state', code: 500`, answering every expired link with a server error"
      ]
    },
    {
      "id": 7,
      "number": "3.3",
      "section": "Enforce it independently of the sweeper",
      "title": "Render the expired state in `SecretRequestFill.vue` as an expiry message rather than an unexpected error.",
      "status": "done",
      "acceptance_criteria": []
    },
    {
      "id": 8,
      "number": "4.1",
      "section": "Tests",
      "title": "PHPUnit for the expire transition: placeholder deleted for a fresh request, Secret and values preserved for a re-request, `expired` is terminal, and the audit actor is the system.",
      "status": "done",
      "acceptance_criteria": []
    },
    {
      "id": 9,
      "number": "4.2",
      "section": "Tests",
      "title": "PHPUnit for the job: sweeps only lapsed pending requests, never touches a request with no `expires_at`, and is idempotent across consecutive runs.",
      "status": "done",
      "acceptance_criteria": []
    },
    {
      "id": 10,
      "number": "4.3",
      "section": "Tests",
      "title": "PHPUnit for the access gate: a lapsed-but-unswept request is refused on `expires_at` alone, an `expired` request reports expiry rather than a 500, and every other status keeps the code it returns today.",
      "status": "done",
      "acceptance_criteria": []
    },
    {
      "id": 11,
      "number": "4.4",
      "section": "Tests",
      "title": "Vitest for the dialog and fill view: expiry pre-filled, clearable to null, and the expired state renders as an expiry message.",
      "status": "done",
      "acceptance_criteria": []
    },
    {
      "id": 12,
      "number": "4.5",
      "section": "Tests",
      "title": "Verify each new/modified spec scenario is driven by a test or carries a reason-bearing `@e2e exclude`, so gate-19 measures this change.",
      "status": "done",
      "acceptance_criteria": []
    },
    {
      "id": 13,
      "number": "5.1",
      "section": "Quality",
      "title": "Translate every new UI string into all 36 locales so the no-regression parity ratchet stays green (`check-l10n.js` and `check-l10n-parity.js` both exit 0).",
      "status": "done",
      "acceptance_criteria": []
    },
    {
      "id": 14,
      "number": "5.2",
      "section": "Quality",
      "title": "Run the full sweep \u2014 hydra gates, PHPUnit, vitest, phpcs, php-cs-fixer, `openspec validate --strict` \u2014 and confirm a `@spec` anchor or reason-bearing exclude on every changed method.",
      "status": "done",
      "acceptance_criteria": []
    }
  ]
}

/**
 * Maps secret type names to vue-material-design-icons component names.
 * Use these to dynamically import or display the correct icon per type.
 */
export const typeIconMap = {
	login: 'Key',
	api_key: 'CodeBraces',
	ssh_key: 'Console',
	certificate: 'ShieldLock',
	note: 'NoteText',
	database: 'Database',
}
